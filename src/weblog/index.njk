---
layout: base.njk
title: Weblog
permalink: /weblog/
---
<section class="writing-index">
  <ol class="post-list">
    {% for post in collections.org %}
    <li class="post-list-item">
      <a href="/weblog/{{ post.data.slug | stripLeadingSlash }}/" class="post-list-link" data-date="{{ post.date | isoDate | replace("-", "_") }}"><span class="post-list-date">{{ post.date | isoDate | replace("-", "_") }}</span><span class="post-list-title">{{ post.title }}{% if post.tags %}<span class="post-list-tags"> // {% for tag in post.tags %}#{{ tag }}{% if not loop.last %} {% endif %}{% endfor %}</span>{% endif %}</span></a>
    </li>
    {% endfor %}
  </ol>

</section>
